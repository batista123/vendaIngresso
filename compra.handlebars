<div class="card mt-4">
        <div class="card-body">
<form action="/usuarios/categorias/deletar"method="POST">
            <input type="hidden" name="id" value="{{_id}}">
            <button type="submit" class="btn btn-danger mt-4">Deletar</button>
        </form>
</div>
    </div>